name: flutter_datetime_format
description: "Local override with formatWithNames shim."
version: 0.0.4
publish_to: "none"

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.4.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.1

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  # Pure Dart; no plugin platforms required.
